// let sortedOrder = todo.sort((a, b) => {
// 	const orderA = dataSwitch ? a.id : -a.id;
// 	const orderB = dataSwitch ? b.id : -b.id;
// 	return orderA - orderB;
// });

//  {sortedOrder.map((curTask, index) => {
// 				return (
// 					<TodoList
// 						key={curTask.id}
// 						index={index}
// 						taskId={curTask.id}
// 						date={curTask.date}
// 						data={curTask.content}
// 						checked={curTask.checked}
// 					/>
// 				);
// 			})}
